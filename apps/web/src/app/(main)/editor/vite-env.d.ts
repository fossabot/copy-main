/**
 * @file vite-env.d.ts
 * @description ملف إعلان أنماط بيئة Vite. يوفّر تعريفات TypeScript لميزات Vite مثل
 *   `import.meta.env.VITE_*` واستيراد الأصول الثابتة (صور، خطوط، CSS modules).
 *   يُولَّد تلقائياً بواسطة Vite ولا يتطلب تعديلاً يدوياً.
 *
 * @see https://vitejs.dev/guide/env-and-mode.html
 */
/// <reference types="vite/client" />
