# Security Guidelines - دليل الأمان

## 🔒 نظام الفحص الأمني التلقائي

تم إعداد نظام فحص أمني شامل يعمل تلقائياً مع كل commit لضمان عدم تسريب أي معلومات حساسة.

### 🚀 الفحص التلقائي

#### Pre-commit Hook
يتم تشغيل فحص أمني سريع قبل كل commit:
```bash
# يتم تشغيله تلقائياً
git commit -m "your message"
```

#### الفحص المتقدم
للفحص الشامل للمشروع بالكامل:
```bash
./scripts/advanced-security-scan.sh
```

### 🛠️ الإعداد الأولي

#### 1. إعداد git-secrets
```bash
./scripts/setup-git-secrets.sh
```

#### 2. تشغيل الفحص الأول
```bash
./scripts/security-scan.sh
```

## 🔍 أنواع الفحص

### 1. فحص الأسرار المكشوفة
- كلمات المرور المكتوبة في الكود
- مفاتيح API
- رموز الوصول (tokens)
- معلومات قواعد البيانات
- شهادات SSL

### 2. فحص الملفات الحساسة
- ملفات `.env` غير المحمية
- مفاتيح SSH
- شهادات SSL
- ملفات التكوين الحساسة

### 3. فحص التبعيات
- الثغرات الأمنية في المكتبات
- الإصدارات القديمة
- التبعيات غير الآمنة

### 4. فحص التكوين
- إعدادات التطوير في الإنتاج
- المنافذ المكشوفة
- الخدمات غير الآمنة

## 🚨 التعامل مع المشاكل الأمنية

### إذا تم اكتشاف مشكلة أمنية:

#### 1. مشاكل حرجة (Critical)
```bash
# إيقاف الـ commit
git commit --no-verify  # فقط في الحالات الطارئة

# إصلاح المشكلة
# 1. إزالة المعلومات الحساسة
# 2. استخدام متغيرات البيئة
# 3. إضافة الملفات للـ .gitignore

# إعادة المحاولة
git commit -m "fix: remove sensitive data"
```

#### 2. تحذيرات (Warnings)
- راجع التحذيرات
- خطط لإصلاحها في المستقبل
- يمكن المتابعة بالـ commit

### إزالة الأسرار من التاريخ
إذا تم اكتشاف سر في التاريخ:
```bash
# استخدم BFG Repo-Cleaner أو git filter-branch
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch path/to/sensitive/file' \
  --prune-empty --tag-name-filter cat -- --all
```

## 📋 قائمة المراجعة الأمنية

### قبل كل commit:
- [ ] لا توجد كلمات مرور مكشوفة
- [ ] لا توجد مفاتيح API مكشوفة
- [ ] لا توجد ملفات `.env` في الـ staging
- [ ] جميع المعلومات الحساسة في متغيرات البيئة

### قبل كل push:
- [ ] تم تشغيل الفحص المتقدم
- [ ] تم مراجعة تقرير الأمان
- [ ] تم إصلاح جميع المشاكل الحرجة

### شهرياً:
- [ ] فحص شامل للمشروع
- [ ] تحديث التبعيات
- [ ] مراجعة إعدادات الأمان
- [ ] فحص التاريخ للأسرار

## 🔧 الأدوات المستخدمة

### 1. Scripts مخصصة
- `security-scan.sh` - فحص سريع
- `advanced-security-scan.sh` - فحص شامل
- `setup-git-secrets.sh` - إعداد git-secrets

### 2. GitHub Actions
- فحص تلقائي مع كل push
- فحص أسبوعي مجدول
- تقارير أمنية تلقائية

### 3. أدوات خارجية
- **git-secrets** - منع تسريب الأسرار
- **Gitleaks** - فحص التاريخ
- **Snyk** - فحص التبعيات
- **CodeQL** - تحليل الكود

## 🚫 ما يجب تجنبه

### ❌ لا تفعل:
```bash
# لا تضع أسراراً في الكود
const API_KEY = "sk-1234567890abcdef"
const PASSWORD = "mySecretPassword"

# لا تضع معلومات قواعد البيانات
const DB_URL = "postgresql://user:pass@host:5432/db"

# لا تستخدم --no-verify إلا في الطوارئ
git commit --no-verify
```

### ✅ افعل:
```bash
# استخدم متغيرات البيئة
const API_KEY = process.env.API_KEY
const PASSWORD = process.env.PASSWORD

# استخدم ملفات .env (مع .gitignore)
# .env
API_KEY=sk-1234567890abcdef
PASSWORD=mySecretPassword

# في .gitignore
.env
.env.local
.env.production
```

## 📞 الإبلاغ عن المشاكل الأمنية

إذا اكتشفت مشكلة أمنية:

1. **لا تنشرها علناً**
2. أرسل تقريراً خاصاً للفريق
3. انتظر الرد قبل الكشف
4. ساعد في الإصلاح إذا أمكن

## 📚 مصادر إضافية

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [GitHub Security Best Practices](https://docs.github.com/en/code-security)
- [Node.js Security Best Practices](https://nodejs.org/en/docs/guides/security/)
- [Git Secrets Documentation](https://github.com/awslabs/git-secrets)

---

**تذكر:** الأمان مسؤولية الجميع! 🔒